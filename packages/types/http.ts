export enum HTTP_CODE {
	BAD_REQUEST = 400,
	UNAUTHORIZED = 401,
	INTERNAL_EXCEPTION = 500,
}
export enum REQUEST_TYPE {
	XHR = 'xhr',
	FETCH = 'fetch',
}

export enum REQUEST_METHOD {
	Get = 'GET',
	Post = 'POST',
	Put = 'PUT',
	Delete = 'DELETE',
}
